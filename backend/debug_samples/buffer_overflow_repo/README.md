# Buffer Overflow Sample

Tiny C target used by the SmartFuzzQL developer lab. It intentionally copies
attacker-controlled input into a fixed-size stack buffer with `strcpy`.

This directory is treated as an internal sample repository through the
`sample://buffer-overflow` URL. Do not use this code as a real application
example.
